const users = require('./users');
const places = require('./places');
const courts = require('./courts');
const courtImages = require('./court-Images');
const placeImages = require('./place-Images');

module.exports = {
    users,
    places,
    courts,
    courtImages,
    placeImages
};